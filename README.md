# Sorting-Algorithm-Comparison
A C++ implementation comparing sorting algorithms, measuring execution time and operations.

Para compilar, basta usar o comando "make".
Para limpar a compilação, basta usar o comando "make clean"

Ao executar o programa, todas as tabelas serão criadas na pasta "tables".